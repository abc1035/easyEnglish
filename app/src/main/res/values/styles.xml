<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ButtonNoShadowStyle" parent="@style/Widget.AppCompat.Button.Borderless"/>

    <style name="listviewstyle">
        <item name="android:cacheColorHint">@null</item>
        <item name="android:divider">@color/line_color</item>
        <item name="android:dividerHeight">1px</item>
        <item name="android:fadingEdge">none</item>
        <item name="android:listSelector">@android:color/transparent</item>
        <item name="android:scrollbarThumbVertical">@color/pressed_bgcolor</item>
        <item name="android:scrollbarSize">2dp</item>
    </style>

    <style name="MyDialogStyle" parent="Theme.AppCompat.Light.DarkActionBar">
        <!--背景设置 为透明-->
        <item name="android:windowBackground">@color/colorTransparentBackground</item>
        <!--设置窗口是半透明的-->
        <item name="android:windowIsTranslucent">true</item>
        <!--设置窗口无标题-->
        <item name="android:windowNoTitle">true</item>
        <!--关闭动画-->
        <item name="android:windowAnimationStyle">@null</item>
    </style>

</resources>